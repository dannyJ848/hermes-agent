[INJECTED BEHAVIORAL TIPS — auto-selected high-confidence]
  execute_code: When using execute_code for multi-step t... → Use execute_code (not terminal) for Python with tool calls. Import from hermes_tools.
  execute_code: When writing long Python functions in ex... → Break into smaller functions. Use write_file first, then import/execute.
  web_extract: When web_extract takes >5s on a URL... → Use max_chars=3000 to limit response. For 403 errors, switch to web_research instead.
  execute_code: When connecting to services from execute... → Check if service is running (docker ps, ps aux). Verify port and URL.
  patch: When making targeted edits to existing f... → Use patch(mode='replace') with enough context lines for unique matching. Include 3+ surrounding lines.
  terminal: When calling terminal with large payload... → Use pagination, reduce output size, or split into smaller calls [CAUTION: 70 failures recorded — verify applicability]
  read_file: When read_file fails with 'file not foun... → Verify path with search_files first. Use Path.home() not ~. Check case sensitivity.
  read_file: When reading large files... → Use offset and limit parameters. Read in chunks of 200-500 lines. Don't read entire files.
  terminal: When accessing files with terminal... → Verify path exists before directory navigation/creation. Use Path.home() not ~ in Python.
  execute_code: When writing Python in execute_code... → Use write_file for multi-line code, not terminal. Check indentation carefully.
  execute_code: When parsing JSON responses from execute... → Use json.loads with strict=False, or try extracting with regex first. Check for BOM markers.
  web_extract: When fetching from restricted URLs with ... → Switch to web_research for the same topic. 403 = anti-bot protection, retrying won't help.
  terminal: When reading file contents via terminal... → Use read_file instead of cat/head/tail — it has pagination and line numbers. [CAUTION: 70 failures recorded — verify app...
  skill_manage: When designing reusable skills for agent... → 7 design patterns for agentic skills: (1) Metadata-driven disclosure — skills declare capabilities; (2) Code-as-skill — ...
  memory: When managing agent memory across sessio... → Implement dual-layer memory: (1) Active layer — high-recency, high-frequency items with slow decay; (2) Archive layer — ...
  write_file: When using write_file for large or multi... → Always verify the write succeeded by reading back the first and last few lines with read_file. write_file OVERWRITES com...
  delegate_parallel: When using delegate_parallel for researc... → Give each parallel task a UNIQUE id and self-contained context. Subagents have ZERO shared memory — each must receive AL...
  delegate_task: When delegating a task to a subagent... → Keep parent context lean — delegate entire workflows, not individual steps. Sub-agent architecture shows >90% context to...
  browser_navigate: When automating browser interactions eff... → Browser automation strategy: (1) Use browser_navigate first to load page and get snapshot; (2) Use refs from snapshot fo...
  patch: When debugging code generated by AI agen... → AI code debugging methodology: (1) Reproduce the error — run the failing test; (2) Read the error message carefully — 80...
  pytest: When When writing pytest tests that import from local .py files → Use importlib.util.spec_from_file_location() to load modules from absolute paths instead of relying on sys.path manipulation. Regular imports fail in pytest collection.
  training_gym: When When training gym exercises use 'echo done' as verify_cmd → The gym measures confidence not competence. Replace with real pytest assertions, file checks, or output comparisons. Only exercises with actual verification teach real skills.
  csv_parsing: When When building CSV parsers with quoted fields → Handle the state machine explicitly: track in_quotes boolean, handle doubled quotes ("") as escaped quotes, and be careful with trailing quotes at field boundaries.
  concurrent_programming: When When building thread-safe data structures in CPython → Use threading.Lock for correctness (GIL makes Lock-based counters faster than CAS for individual ops). For 100K+ ops, Lock achieves >2M ops/sec easily.
  sql_engine: When When building mini SQL engines for testing → Verify against SQLite as ground truth. Build a sqlite_query() reference function and compare results parametrically. This catches subtle ordering and type mismatches.
  flask_testing: When When testing Flask apps with pytest → Use app.test_client() fixture. Reset shared state (like global lists) between tests with a fixture that clears them. Import the app module dynamically to avoid circular imports.
  pytest: When When loading multi-file Python packages in pytest THEN Remove relative imports (from .core import X) and use importlib.util to load each module individually. Relative imports fail with importlib.spec_from_file_location because there's no parent package context.
  rate_limiter: When When building rate limiters that need reliable tests THEN Use time.monotonic() (not time.time()) for timing. Token bucket pattern with explicit refill calculation avoids timing drift. Add sleep margin of 25%+ over the window for reliability.
  rest_api_testing: When When testing REST APIs with Flask test_client THEN Use fixture that resets global state (dbs, counters) between tests. Verify response schema structure (assert 'error' in error) not just status codes. Test CRUD end-to-end in single test for integration confidence.
  concurrent_testing: When When testing concurrent code with threading THEN Count exact allowed vs blocked requests. Use threading.Lock for shared result lists. Verify sum equals total requests (no lost operations). Run stress test 3x to catch race conditions.