  execute_code: When using execute_code for multi-step tool logic that requires structured output → Use execute_code (not terminal) for Python with tool calls. Import from hermes_t
  execute_code: When writing long Python functions in execute_code → Break into smaller functions. Use write_file first, then import/execute.
  execute_code: When connecting to services from execute_code → Check if service is running (docker ps, ps aux). Verify port and URL.
  terminal: When calling terminal with large payloads → Use pagination, reduce output size, or split into smaller calls [CAUTION: 70 fai
  terminal: When accessing files with terminal → Verify path exists before directory navigation/creation. Use Path.home() not ~ i
  terminal: When reading file contents via terminal → Use read_file instead of cat/head/tail — it has pagination and line numbers. [CA
  web_extract: When web_extract takes >5s on a URL → Use max_chars=3000 to limit response. For 403 errors, switch to web_research ins
  web_extract: When fetching from restricted URLs with web_extract → Switch to web_research for the same topic. 403 = anti-bot protection, retrying w
  web_extract: When parsing documents efficiently with OCR → AgenticOCR: Parse only what you need. Instead of full OCR on entire documents: (
  web_research: When searching the web for current information → Web research optimization: (1) Use specific queries — broad queries return noise
  web_research: When using AI-powered search for information retrieval → AI Search paradigm: Traditional search is query → rank → retrieve. AI search add
  web_research: When web_research finds relevant results for tool-use optimization → Follow up with web_extract on the top 2 URLs, then use execute_code to synthesiz
  delegate_with_model: When selecting models for cost-performance tradeoffs → CASTER: Breaking cost-performance barrier in multi-agent systems. Route tasks by
  delegate_with_model: When delegating research tasks to models via delegate_with_model → Free models (nemotron-free, qwen-coder-free, mimo-v2-pro) handle 80% of tasks. O
  delegate_with_model: When routing agents based on learned skills → SkillOrchestra: Learn fine-grained skills from execution experience. (1) Each ag
  delegate_task: When delegating a task to a subagent → Keep parent context lean — delegate entire workflows, not individual steps. Sub-
  delegate_task: When spawning subagents for parallel isolated work → Subagent delegation: (1) Subagents have NO memory — pass ALL context via context
  delegate_task: When coordinating multiple agents with self-evolving protocols → SECP (Self-Evolving Coordination Protocol): Agents can evolve their own coordina
  delegate_parallel: When using delegate_parallel for research tasks that requires structured output → Give each parallel task a UNIQUE id and self-contained context. Subagents have Z
  delegate_parallel: When identifying parallelizable subtasks for concurrent execution → Parallel task identification: (1) Two subtasks are parallelizable if they share 
  delegate_parallel: When orchestrating multi-agent workflows as DAGs → Model agentic workflows as DAGs: (1) Nodes = agent tasks; (2) Edges = data depen
  read_file: When read_file fails with 'file not found' → Verify path with search_files first. Use Path.home() not ~. Check case sensitivi
  read_file: When reading large files → Use offset and limit parameters. Read in chunks of 200-500 lines. Don't read ent
  read_file: When reading files efficiently without overwhelming context → File reading optimization: (1) ALWAYS use offset and limit for large files; (2) 
  write_file: When using write_file for large or multi-part generated content → Always verify the write succeeded by reading back the first and last few lines w
  write_file: When using write_file for: write: plugin.yaml that requires structured output → write_file auto-creates parent directories — no need for mkdir first
  write_file: When writing files that will be read by other agents or processes → File writing for agent consumption: (1) Include a header comment explaining the 
  patch: When making targeted edits to existing files → Use patch(mode='replace') with enough context lines for unique matching. Include
  patch: When debugging code generated by AI agents → AI code debugging methodology: (1) Reproduce the error — run the failing test; (
  patch: When patch fails to find the old_string in a file → Read the file first to get the EXACT text including whitespace. The fuzzy matche
  search_files: When searching file contents or finding files by name → Search files mastery: (1) target='content' for regex inside files; (2) target='f
  search_files: When navigating unfamiliar codebases effectively → Codebase navigation strategy: (1) Start from entry points (main, index, app); (2
  search_files: When redefining retrieval evaluation for agent search tools → LLM-era retrieval evaluation: Traditional metrics (MRR, NDCG) don't capture whet
  memory: When managing agent memory across sessions with growing state → Implement dual-layer memory: (1) Active layer — high-recency, high-frequency ite
  memory: When designing agent memory architectures for long-term use → Agent memory types (from 𝕋ℝ𝑀𝑒𝑚): (1) Semantic — facts and concepts (like our dis
  memory: When designing multi-layer memory for long-term agent persistence → Multi-layered memory architecture: Layer 1 (Working) — current conversation, in-
  session_search: When retrieving episodic memory from past sessions → Episodic memory is the missing piece: Semantic memory knows 'what', but episodic
  session_search: When searching conversation history for relevant past context → Conversation history search: (1) Use broad keywords with OR for recall; (2) Narr
  session_search: When using recursive summarization for long-term memory → Recursive summarization: For very long conversations: (1) Summarize every N mess
  skill_manage: When designing reusable skills for agent systems → 7 design patterns for agentic skills: (1) Metadata-driven disclosure — skills de
  skill_manage: When building and managing agentic skills beyond simple tool use → SoK: Agentic Skills — Skills go beyond tool use. Tool use = call an API. Skill =
  skill_manage: When managing skill security and trust → ClawHavoc case study: 1,200 malicious skills infiltrated a marketplace, exfiltra
  validate_output: When validating delegation results before trusting them → Output validation: (1) ALWAYS validate after delegate_with_model; (2) Checks: ha
  validate_output: When evaluating agent performance across diverse tasks → Apply the 5-level evaluation taxonomy: L1=Agentic Skills (tool use), L2=Domain-A
  validate_output: When detecting hallucinations in LLM agent outputs → Hallucination detection best practices: (1) Cross-reference claims against multi
  context: When managing agent context window across long sessions → By turn 30, input tokens per call can be 5-10x what they were at turn 1. Most of
  context: When the agent context window is 50%+ full and approaching limits → Aggressively compress completed work into summary lines. Keep only: (1) the curr
  context: When managing reasoning token costs in agent loops → Use a Shifted Thinking Window approach: cap reasoning tokens per turn and strip 