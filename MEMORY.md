DGX Hermes deployment (May 16-17 2026): vLLM serves Qwen3.6-27B-Uncensored + dynamic LoRA + DFlash speculative decoding. Tool calling broken: Qwen outputs XML tool format but vLLM Hermes parser expects JSON. Workaround: text-based tool execution wrapper at /tmp/autonomous_runner_v2.py. User rejected systemd daemons - use screen/tmux only. Full capabilities: local write, MacBook SSH+write, web access, browser automation, Git, Docker. Model must be full path /data/models/Qwen3.6-27B-Uncensored. Speed: ~6.2 tok/s with speculative, ~12 tok/s without.
§
YantrikDB ingest queue bug (May 17, 2026): The background thread can permanently stop draining the 256-op queue. `think()` takes 18+ seconds without reducing queue depth. Workaround: direct SQLite insertion using `db.embed()` for embeddings, then `struct.pack(f'{len(emb)}f', *emb)` for the blob. Schema: 30 columns in `memories` table. Post-insertion: call `db.rebuild_vec_index()`. Also: `record_batch()` takes list of dicts, no `namespace` kwarg. `recall()` with `top_k > 1000` on large DBs hits SQLite's 32,766 parameter limit in IN clause.
§
User expects git commits to be pushed to their online GitHub repo (dannyJ848/hermes-agent) immediately after making changes. Does not want local-only commits left unpushed. Branch: qwen27b-training-artifacts-may3-2026.
§
DGX Hermes Qwen tool calling issue (May 17 2026): Qwen3.6-27B-Uncensored generates tool calls in XML format (<tool_call><function=name>) but vLLM Hermes parser expects JSON. This causes empty tool_calls arrays. Workaround: text-based tool execution wrapper at /tmp/autonomous_runner_v2.py parses XML format and executes tools manually. vLLM error: JSONDecodeError in hermes_tool_parser.py line 114. Model requires full path /data/models/Qwen3.6-27B-Uncensored in config.
§
Gateway module shadowing fix (May 17, 2026): `hermes_cli/gateway.py` shadows `gateway/` package. Fix: pre-import `gateway` via `importlib.util` before `from gateway import ...`. Same for `plugins/` if `hermes_cli/plugins.py` exists. Applied to MacBook and DGX. Also: YantrikDB ingest queue workaround — direct SQLite insertion with `db.embed()` + `struct.pack()` for embeddings, bypass broken background thread. Schema: 30 columns in `memories` table.